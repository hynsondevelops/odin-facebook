<h1> Welcome to Fakebook, <%= @signed_in.first_name %></h1>
<h3> Friend List </h3>
<div id = "friendList">
	<% @signed_in.friends.each do |friend| %>
		<p> Name: <%= friend.first_name %> <%= friend.last_name %> </p>
		<p> Email: <%= friend.email %> </p>

	<% end %>
</div>

<div id = "friendRequestsBox">
	<p> Friend Requests </p>
	<% if @friend_requests.size == 0 %>
		<p> No friends :( </p>
	<% end %>
	<% @friend_requests.each do |request| %>
		<p> Name: <%= request.friender.first_name %> <%= request.friender.last_name %>  </p>
	<% end %>
</div>

<div id = "feed">
	<% @feed.each do |cur_post| %>
	  <%= render "post", post: cur_post %>
	<% end %>
</div>
